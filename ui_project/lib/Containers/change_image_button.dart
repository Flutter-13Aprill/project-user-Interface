import 'package:flutter/material.dart';

// This class is the change button.
class ChagngeImageButton extends StatelessWidget {
  const ChagngeImageButton({super.key});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(onPressed: () {}, child: const Text("Change Image"),);
  }
}
